The mother of all redux examples
================================

Just a counter example. Nothing much to see here, like seriously! Move it along already :)

Install by running `npm install`  
Start by running `npm start`
