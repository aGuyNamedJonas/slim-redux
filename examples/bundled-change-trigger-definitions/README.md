Bundled change trigger definitions example
==========================================

This example is probably not even needed. Enjoy nonetheless!

Install by running `npm install`  
Start by running `npm start`
